scalar Time

type Order {
    ID: Int
    TotalPrice: Float
    UpdatedAt: Time
    CreatedAt: Time
}

type Cart {
    ID: Int
    ItemsID: Float
    Quantity: Int
    UpdatedAt: Time
    CreatedAt: Time
}

type Query {
}

type Mutation {
    AddCart(sku: String, quantity: Int): Cart
    ConfirmOrder(): Order
}
